# PRD：照片滑动清理 iOS App

## 1. 产品背景 & 目标

### 1.1 背景
- 手机照片数量持续增长，用户整理成本极高  
- 系统相册删除路径较深，误删心理成本高  
- 用户希望：**快速决策 + 可反悔 + 批量确认**

### 1.2 产品目标
- 用极低的认知成本帮助用户快速清理照片  
- 通过「暂存删除」机制降低误删焦虑  
- 整体体验强调：快、顺、无打断  

### 1.3 核心价值
> 将「是否保留一张照片」压缩为 1 秒内的手势决策

---

## 2. 目标用户 & 使用场景

### 2.1 目标用户
- 照片数量 5,000+ 的 iPhone 用户  
- 经常拍照但极少整理（截图、重复照、糊片）  
- 对效率敏感，不喜欢频繁点击按钮  

### 2.2 核心使用场景
- 睡前 / 通勤 / 等人等碎片时间  
- 想“随手刷一刷，把明显垃圾照片删掉”  
- 希望最终统一确认，而非即时删除  

---

## 3. 核心功能概览

| 功能 | 描述 |
|----|----|
| 照片浏览 | 单张照片全屏展示 |
| 左滑 / 右滑 | 浏览上一张 / 下一张 |
| 上滑 | 将照片加入暂存删除区 |
| 暂存计数 | 实时显示待删除数量 |
| 删除确认 | 一次性真实删除 |
| 撤销操作 | 支持误删回退 |

---

## 4. 核心交互设计

### 4.1 照片浏览与手势定义

#### 4.1.1 左右滑动
- 左滑：切换到下一张照片  
- 右滑：切换到上一张照片  
- 滑动需有平滑过渡动画  
- 滑动过程中可显示相邻照片边缘预览  

#### 4.1.2 上滑删除（暂存）
- 上滑手势触发「加入暂存删除区」  
- 动画效果：
  - 照片向上飞出  
  - 页面底部短暂提示「已加入删除」  
- 操作完成后自动展示下一张照片  

> 注意：上滑不会立即调用系统删除接口

---

### 4.2 暂存删除机制（核心）

#### 4.2.1 暂存区定义
- 被上滑的照片进入「待删除列表」  
- App 内维护照片 Asset ID 集合  
- 不影响系统相册真实数据  

#### 4.2.2 暂存数量展示
- 页面固定区域显示：
  - 示例：🗑 待删除：12  
- 数量变化需有轻微动效反馈  

#### 4.2.3 撤销机制
- 上滑后 3~5 秒内支持撤销  
- 形式：
  - Toast：「已加入删除」+「撤销」  
- 撤销后照片回到原浏览序列  

---

## 5. 删除确认流程

### 5.1 入口规则
- 页面展示「确认删除」按钮  
- 当暂存数量 = 0 时按钮置灰  

### 5.2 删除确认页
展示内容：
- 待删除照片缩略图列表（可横向滑动）  
- 文案提示：`即将删除 12 张照片`  

操作项：
- 确认删除  
- 取消返回  

### 5.3 二次确认弹窗
系统弹窗文案示例：
> “确认从系统相册中删除这 12 张照片？  
> 删除后可在系统「最近删除」中找回。”

---

## 6. 删除行为定义（iOS）

### 6.1 技术约束
- 使用 `Photos.framework`  
- 调用 `PHAssetChangeRequest.deleteAssets`  
- 删除后照片进入系统「最近删除」  

### 6.2 异常处理

| 场景 | 处理方式 |
|----|----|
| 未授权相册权限 | 引导用户前往系统设置授权 |
| 删除失败 | 明确提示失败数量 |
| 部分成功 | 区分成功 / 失败照片 |

---

## 7. 增强功能（非 MVP）

### 7.1 照片筛选
- 仅查看截图  
- 仅查看视频  
- 按日期区间筛选  

### 7.2 辅助信息
- 显示拍摄时间  
- 显示文件大小  

### 7.3 成就反馈
- 删除完成后提示：
  - 示例：「已释放 1.2 GB 空间」
- 简单动效增强正反馈  

---

## 8. 状态与边界条件

### 8.1 无可浏览照片
- 显示空状态页：
  - 「已经没有更多照片了」
- 引导用户确认删除或退出  

### 8.2 全部被暂存删除
- 浏览区不再显示照片  
- 仅允许：
  - 撤销部分删除  
  - 确认删除  

### 8.3 App 中断处理
- App 进入后台或被杀进程  
- 暂存删除列表需本地持久化  

---

## 9. 数据埋点建议

| 事件 | 说明 |
|----|----|
| swipe_left / swipe_right | 浏览行为 |
| swipe_up | 删除意图 |
| undo_delete | 误操作率 |
| confirm_delete | 核心转化 |
| abandon_delete | 犹豫 / 放弃行为 |

---

## 10. 成功指标（KPI）

- 单次会话平均删除照片数  
- 上滑 → 确认删除转化率  
- 撤销比例  
- 日均清理照片数量  

---

## 11. 产品备注（风险提示）

1. 上滑即删除是高风险手势，必须依赖暂存 + 撤销机制兜底  
2. MVP 阶段不宜引入多宫格浏览，保持单一照片流  
3. 性能和流畅度优先级高于功能丰富度  
4. 相册权限弹窗触发时机需谨慎设计  

---
 
 
